#include <iostream>

using namespace std;
int main() {
    int sum, x, y;
    // Insertion operator (<<-->) bit right shift operator)
    // Insert this in output stream
    cout<<"Enter any number: ";
    // Extraction operator (>>--<)bit left shift operator
    // Extract input from input stream
    cin>>x;
    // 
    cout<<"Enter any number: ";
    cin>>y;
    sum = x + y;
    cout<<"Sum of two numbers is "<<sum<<"\n";

}